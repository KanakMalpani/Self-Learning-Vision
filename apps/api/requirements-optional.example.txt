# Optional provider integrations
#
# Self-Learning Vision runs without this file. The default install uses local/open-source
# dependencies only. Use this file as a starting point when adding paid or hosted providers.
#
# Example SDKs you may choose to install for custom provider implementations:
# cryptography is already included by default for encrypted privacy vault exports.
# openai>=1.0.0
# anthropic>=0.34.0
# google-genai>=0.3.0
# replicate>=0.31.0
