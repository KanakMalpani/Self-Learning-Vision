{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal webview permissions for Self-Learning Vision.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
